apiVersion: v1
kind: Pod
metadata:
  name: mypod
spec:
  containers:
  - name: mypod
    image: redis
    volumeMounts:
    - name: foo
      mountPath: "/Users/rajeev/kubernetes_demo/configmaps_secrets"
      readOnly: true
  volumes:
  - name: foo
    configMap:
      name: myconfigmap
